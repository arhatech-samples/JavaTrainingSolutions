package com.techarha.training.java.innerclasses.question;

/**
 * @question1 Create an interface with at least one method, and implement that interface by defining an inner class
 *           within a method, which returns a reference to your interface.
 * @explaination1 The inner class must be created inside a method not under class.
 *
 * @question2 Now try to create the inner class inside a scope (example inside if block) instead of method.
 *
 * @author ankit.
 */
public class LocalInner {
    //TODO implement the code for above question
}
