package com.techarha.training.java.enums;

/**
 * @question1 Create an enum of the least-valuable six types of paper currency. Loop through the values( ) and print
 *           each value and its ordinal( ).
 * @explaination1 declare enum inside class and then call values() function to loop through its values.
 *
 * @question2 Write a switch statement for the enum in the previous example. For each case, output a description of that
 *           particular currency.
 * @explaination2 pass enum variable created in switch and use possible enum constants as case constants.
 *
 * @author ankit.
 */
public class enums {

    //TODO complete above queries
}
